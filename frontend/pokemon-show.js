const $pokemon = document.querySelector(".card")

//fetch("http://localhost:4000/pokemons")
//.then(response => response.json())
//.then((res) => {
    // res.forEach(pokemon => {
     //   let showitem = document.createElement("img")
    //    showitem.setAttribute("src",`${pokemon.image}`)
    //    $pokemon.appendChild(showitem)
   // })
//});